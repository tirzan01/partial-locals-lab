Classroom Info

<%= render partial: 'classroom.html.erb', locals: { classroom: @classroom } %>

<%= @classroom.students.each do |s| %>
  <%= render partial: 'students/student', locals: { student: s } %>
<% end %>
